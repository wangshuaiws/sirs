server:
  port: 8080

spring:
  application:
    name: sirs-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/sirs
    username: sirs
    password: sirs123
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto

# JWT
sirs:
  jwt:
    secret: sirs-jwt-secret-key-change-in-production-20250713-this-makes-it-64-bytes-long
    expiration-days: 7

# Logging
logging:
  level:
    com.sirs: DEBUG
    org.springframework.security: INFO
